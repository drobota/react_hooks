import * as React from "react";

interface BulletedListProps {
  title: string;
  items: string[];
}
//
// const BulletedList: React.FC<BulletedListProps> = (props) => {
//   return (
//     <ul className="">{this.title }</ul>
//   );
// };